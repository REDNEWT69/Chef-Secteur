(function(){
  'use strict';
  const DAYS=['Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi'];
  function norm(v){return String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim()}
  function monday(){try{const raw=(state.settings&&state.settings.weekDate)||new Date().toISOString().slice(0,10),d=new Date(raw+'T12:00:00'),w=d.getDay()||7;d.setDate(d.getDate()-w+1);return d}catch(e){return new Date()}}
  function dateForDay(day){const d=monday();d.setDate(d.getDate()+Math.max(0,DAYS.indexOf(day)));return d.toLocaleDateString('fr-FR',{weekday:'long',day:'2-digit',month:'2-digit',year:'numeric'})}
  function allStores(){try{return (state.stores&&state.stores.length?state.stores:(typeof DEFAULT_STORES!=='undefined'?DEFAULT_STORES:[]))||[]}catch(e){return[]}}
  function plannedDaysForStore(store){const hits=[];try{for(const day of DAYS){const r=(state.plan&&state.plan[day])||[];for(let i=0;i<r.length;i++){const s=r[i];if(String(s.id||'')===String(store.id||'')||(norm(s.enseigne)===norm(store.enseigne)&&norm(s.ville)===norm(store.ville))){hits.push({day,index:i,store:s});break}}}}catch(e){}return hits}
  function findArrival(storeId,day){try{const el=document.querySelector('.stop[data-store-id="'+CSS.escape(String(storeId))+'"][data-day="'+CSS.escape(day)+'"]');if(el&&el.dataset.arrival)return el.dataset.arrival}catch(e){}return null}
  function bestMatches(text){const q=norm(text);if(q.length<3)return[];const common=new Set(['quand','passe','passer','vais','aller','magasin','visite','visiter','programme','programmer','prevu','prevue','chez','dans','quel','quelle','jour','date']);const words=q.split(' ').filter(w=>w.length>2&&!common.has(w));if(!words.length)return[];const scored=[];for(const s of allStores()){const label=norm((s.enseigne||'')+' '+(s.ville||'')+' '+(s.adresse||''));let score=0;for(const w of words)if(label.includes(w))score+=w.length>=6?3:1;if(q.includes(norm((s.enseigne||'')+' '+(s.ville||''))))score+=10;if(score>=3)scored.push({s,score})}scored.sort((a,b)=>b.score-a.score);if(!scored.length)return[];const top=scored[0].score;return scored.filter(x=>x.score===top).slice(0,4).map(x=>x.s)}
  function answer(text){const n=norm(text);if(!/(quand|passe|passer|programme|prevu|date|quel jour|quelle jour|visite)/.test(n)&&n.split(' ').length<2)return null;const matches=bestMatches(text);if(!matches.length)return null;const lines=[];for(const store of matches){const hits=plannedDaysForStore(store);const name=(store.enseigne||'Magasin')+' '+(store.ville||'');if(hits.length){for(const h of hits){const arr=findArrival(store.id,h.day);lines.push(name+' est prévu '+dateForDay(h.day)+(arr?' vers '+arr:'')+' (visite n°'+(h.index+1)+' de la journée).')}}else{lines.push(name+' fait bien partie de ton secteur'+(store.freq?' ('+store.freq+')':'')+', mais il n’est pas planifié dans la semaine actuellement affichée.') }}return lines.join('\n')}
  window.chefSecteurStoreScheduleAnswer=answer;
  function install(){if(window.__storeScheduleAssistantInstalled||typeof window.assistantSend!=='function')return false;const base=window.assistantSend;window.assistantSend=function(){const input=document.getElementById('assistantInput'),text=input&&input.value?input.value.trim():'';if(text){const a=answer(text);if(a){input.value='';if(typeof window.assistantAdd==='function')window.assistantAdd(text,'user');if(typeof window.assistantBot==='function')window.assistantBot(a);return}}return base.apply(this,arguments)};window.__storeScheduleAssistantInstalled=true;return true}
  let tries=0;const timer=setInterval(function(){tries++;if(install()||tries>100)clearInterval(timer)},100);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else setTimeout(install,0);
})();
