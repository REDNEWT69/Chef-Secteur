(function(){
'use strict';
const M=window.StoreRunnerVisitModel;
const STEPS=['Préparation','Arrivée / 360°','Méthode 6P','Conclusion','Plan d’action'];
let dialog,body,status,title,session,activeId=null,viewStep=0,opener=null;
function element(tag,text,cls){const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;return n}
function button(text,fn,cls='secondary'){const b=element('button',text,cls);b.type='button';b.addEventListener('click',fn);return b}
function domain(){return window.state.businessV2||M.empty()}
function current(){return domain().visits.find(x=>x.id===activeId)}
function name(storeId){const s=window.state.stores.find(x=>String(x.id)===storeId)||domain().storeSnapshots[storeId];return s?s.enseigne+' · '+s.ville:'Magasin archivé'}
function message(text,error=false){status.textContent=text;status.classList.toggle('sr-error',error)}
async function save(change,after){try{await session.edit(change);if(after)after();return true}catch(e){message('Non enregistré : '+e.message,true);return false}}
function field(host,label,value,onChange,type='textarea',disabled=false){const wrap=element('label',label,'sr-field'),input=element(type==='textarea'?'textarea':'input');if(type!=='textarea')input.type=type;else input.rows=2;input.value=value||'';input.disabled=disabled;input.addEventListener(type==='date'?'change':'input',()=>onChange(input.value));wrap.append(input);host.append(wrap);return input}
function select(host,label,value,options,onChange,disabled=false){const wrap=element('label',label,'sr-field'),input=element('select');for(const [key,text] of options){const opt=element('option',text);opt.value=key;input.append(opt)}input.value=value;input.disabled=disabled;input.onchange=()=>onChange(input.value);wrap.append(input);host.append(wrap);return input}
function visitField(host,v,section,key,label,value,type='textarea'){return field(host,label,value,val=>save(s=>M.editVisit(s,v.id,section,key,val)),type,v.status==='completed')}
function prepare(host,v){host.append(element('p',name(v.storeId)));const info=element('section',undefined,'sr-context');info.append(element('h4','À reprendre en magasin'));
 const previous=domain().visits.filter(x=>x.storeId===v.storeId&&x.id!==v.id&&x.status==='completed').sort((a,b)=>b.completedAt.localeCompare(a.completedAt));for(const row of previous.slice(0,3))info.append(element('p',row.completedDate+' · '+row.conclusion));if(!previous.length){const legacy=(window.state.visits||{})[v.storeId];info.append(element('p',legacy&&legacy.lastVisit?'Dernière visite historique : '+legacy.lastVisit:'Aucune visite précédente.'))}
 const actions=domain().actions.filter(a=>a.storeId===v.storeId&&!['done','cancelled'].includes(a.status));for(const a of actions)info.append(element('p',a.description+' · '+(a.owner||'Responsable à définir')+' · '+(a.dueDate||'Sans échéance')));if(!actions.length)info.append(element('p','Aucune action en attente.'));
 const next=(window.state.appointments||[]).filter(a=>String(a.storeId)===v.storeId&&a.date>=todayISO()).sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time))[0];info.append(element('p',next?'Prochain rendez-vous : '+next.date+' '+next.time+' · '+(next.subject||next.type||''):'Aucun prochain rendez-vous.'));host.append(info);
 for(const [key,label] of Object.entries(M.PREP))visitField(host,v,'preparation',key,label,v.preparation[key]);
}
function arrival(host,v){M.CHECKS.forEach((label,i)=>{const wrap=element('label',undefined,'sr-check'),input=element('input');input.type='checkbox';input.checked=v.arrival.checks[i];input.disabled=v.status==='completed';input.onchange=()=>save(s=>M.editVisit(s,v.id,'check',i,input.checked));wrap.append(input,document.createTextNode(label));host.append(wrap)});
 visitField(host,v,'arrival','positives','Constats positifs',v.arrival.positives);visitField(host,v,'arrival','opportunities','Opportunités identifiées (notes)',v.arrival.opportunities);
 host.append(element('h4','Anomalies'));for(const a of v.arrival.anomalies){const box=element('section',undefined,'sr-item');field(box,'Anomalie',a.text,value=>save(s=>M.editAnomaly(s,v.id,a.id,value)),'textarea',v.status==='completed');if(v.status==='draft'){const convert=button(a.actionId?'Action déjà créée':'Transformer en action',()=>save(s=>M.actionFromAnomaly(s,v.id,a.id),render));convert.disabled=!!a.actionId;box.append(convert)}host.append(box)}if(v.status==='draft')host.append(button('Ajouter une anomalie',()=>save(s=>M.addAnomaly(s,v.id),render)));
}
function sixP(host,v){for(const [p,section] of Object.entries(M.SIX_P)){const details=element('details');details.dataset.section=p;details.append(element('summary',section.label));section.items.forEach((label,i)=>{const item=v.sixP[p][i],box=element('section',undefined,'sr-item');box.append(element('h4',label));select(box,'Statut',item.status,[['','Non évalué'],['ok','OK'],['correct','À corriger'],['opportunity','Opportunité']],value=>save(s=>M.edit6P(s,v.id,p,i,'status',value)),v.status==='completed');
 for(const [key,label,type] of [['comment','Commentaire','textarea'],['action','Action','textarea'],['owner','Responsable','text'],['dueDate','Échéance','date']])field(box,label,item[key],value=>save(s=>M.edit6P(s,v.id,p,i,key,value)),type,v.status==='completed');
 if(v.status==='draft')box.append(button(item.actionId?'Action liée : synchronisation automatique':'Créer l’action',()=>save(s=>M.actionFrom6P(s,v.id,p,i),render)));details.append(box)});host.append(details)}}
function conclusion(host,v){visitField(host,v,'conclusion',null,'Conclusion de visite',v.conclusion);if(v.status==='completed')host.append(element('p','Visite terminée le '+v.completedDate));else{host.append(element('p','Les actions renseignées dans les 6P seront conservées dans le plan d’action.'));host.append(button('Terminer la visite',()=>save(s=>M.complete(s,v.id,todayISO()),()=>{viewStep=4;render();window.renderAll();message('Visite terminée et historique enregistré.')}),'primary'))}}
function actions(host,v){const rows=domain().actions.filter(a=>a.storeId===v.storeId);if(!rows.length)host.append(element('p','Aucune action. Ajoute une anomalie ou renseigne une action dans les 6P.'));for(const a of rows){const box=element('section',undefined,'sr-item');box.append(element('h4',a.category+' · '+a.description));field(box,'Responsable',a.owner,value=>save(s=>M.editAction(s,a.id,'owner',value)),'text');field(box,'Échéance',a.dueDate,value=>save(s=>M.editAction(s,a.id,'dueDate',value)),'date');const completed=element('p',a.completedAt?'Réalisée le '+new Date(a.completedAt).toLocaleString('fr-FR'):'');select(box,'Avancement',a.status,[['open','À faire'],['in_progress','En cours'],['done','Réalisée'],['cancelled','Annulée']],value=>save(s=>M.editAction(s,a.id,'status',value),()=>{const saved=domain().actions.find(x=>x.id===a.id);completed.textContent=saved.completedAt?'Réalisée le '+new Date(saved.completedAt).toLocaleString('fr-FR'):''}));box.append(completed);host.append(box)}}
function render(){const v=current();if(!v){hub();return}const openSections=new Set([...body.querySelectorAll('details[open][data-section]')].map(x=>x.dataset.section));body.replaceChildren();title.textContent=name(v.storeId)+' · '+(v.status==='draft'?'Visite en cours':'Visite terminée');const nav=element('nav',undefined,'sr-steps');nav.setAttribute('aria-label','Étapes de la visite');const step=v.status==='draft'?v.step:viewStep;STEPS.forEach((label,i)=>{const b=button((i+1)+'. '+label,async()=>{if(!await save())return;if(v.status==='draft')await save(s=>M.editVisit(s,v.id,'step',null,i),()=>{render();dialog.scrollTop=0});else{viewStep=i;render();dialog.scrollTop=0}});b.setAttribute('aria-current',step===i?'step':'false');nav.append(b)});body.append(nav,element('h3',STEPS[step]));[prepare,arrival,sixP,conclusion,actions][step](body,v);for(const d of body.querySelectorAll('details[data-section]'))d.open=openSections.has(d.dataset.section)}
function hub(){activeId=null;title.textContent='Visites et actions';body.replaceChildren();const rows=domain().visits.slice().sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt));if(!rows.length)body.append(element('p','Démarre une visite depuis une fiche magasin, le planning ou la tournée.'));for(const v of rows){const box=element('section',undefined,'sr-item');box.append(element('h3',name(v.storeId)),element('p',v.status==='draft'?'Visite en cours':('Terminée le '+v.completedDate)),button(v.status==='draft'?'Reprendre la visite':'Consulter la visite et ses actions',()=>{activeId=v.id;viewStep=0;render()}));body.append(box)}}
function show(){if(!dialog.open){opener=document.activeElement;dialog.showModal()}}
async function start(storeId){show();let id;await save(s=>{id=M.start(s,String(storeId))},()=>{activeId=id;viewStep=0;render()})}
async function close(){if(!await save())return;dialog.close();if(opener&&opener.isConnected)opener.focus();window.renderAll()}
function boot(){if(document.getElementById('srVisitDialog'))return;dialog=element('dialog');dialog.id='srVisitDialog';dialog.className='sr-visit';dialog.setAttribute('aria-labelledby','srVisitTitle');const header=element('div',undefined,'sr-head');title=element('h2','Visites et actions');title.id='srVisitTitle';header.append(title,button('Fermer',close));status=element('p',undefined,'sr-status');status.setAttribute('role','status');body=element('div');dialog.append(header,status,button('Réessayer l’enregistrement',()=>save()),body);document.body.append(dialog);
 session=window.StoreRunnerVisitStore.create({model:M,reliability:window.ChefReliability,db:window.__chefStorage||window.storage||window.localStorage,getState:()=>window.state,setState:s=>{window.state=s},onStatus:(phase,error)=>{if(phase==='saving')message('Enregistrement…');else if(phase==='error')message('Non enregistré : '+error,true);else message(window.__chefStorageMode==='memory'?'Stockage temporaire : exporte tes données avant de fermer.':'Enregistré localement · '+new Date().toLocaleTimeString('fr-FR'),window.__chefStorageMode==='memory')}});
 dialog.addEventListener('cancel',e=>{e.preventDefault();close()});
 document.addEventListener('click',e=>{const b=e.target.closest('[data-sr-start],[data-sr-current],[data-sr-terrain],[data-sr-hub]');if(!b)return;e.preventDefault();e.stopPropagation();if(b.hasAttribute('data-sr-hub')){show();save(undefined,hub)}else if(b.hasAttribute('data-sr-current')){if(window.currentEditId)start(window.currentEditId)}else if(b.hasAttribute('data-sr-terrain')){const t=window.terrainCurrent();if(t)start(t.store.id);else{show();hub();message('Aucune visite de tournée en attente.')}}else start(b.dataset.srStart)},true);
 document.addEventListener('store-runner:data-restored',()=>{session.invalidate();if(dialog.open){hub();message('Sauvegarde restaurée.')}});
 window.addEventListener('beforeunload',e=>{if(session.hasPending()){e.preventDefault();e.returnValue=''}});
 document.addEventListener('visibilitychange',()=>{if(document.hidden)save()});
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
