(function(){
'use strict';
const DAYS=['Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi'];
const ARCHIVE_KEY='chef_sector_plan_archive_v1';
let viewMonth=null,touchStartX=null,refreshObserver=null,refreshTimer=null;
const observedRefreshTargets=new WeakSet();
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function norm(v){return String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'')}
function parseDate(v){const d=new Date(String(v||'')+'T12:00:00');return isNaN(d)?null:d}
function iso(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}
function monday(d){const x=new Date(d),w=x.getDay()||7;x.setDate(x.getDate()-w+1);return x}
function mins(t){const p=String(t||'').split(':');return (+p[0]||0)*60+(+p[1]||0)}
function clock(v){v=Math.round(v);return String(Math.floor(v/60)%24).padStart(2,'0')+':'+String(v%60).padStart(2,'0')}
function archive(){try{return JSON.parse(localStorage.getItem(ARCHIVE_KEY)||'{}')||{}}catch(e){return{}}}
function baseMonth(){let d=null;try{d=parseDate(state.settings&&state.settings.weekDate)}catch(e){};d=d||new Date();return new Date(d.getFullYear(),d.getMonth(),1,12)}
function currentMonth(){if(!viewMonth)viewMonth=baseMonth();return{year:viewMonth.getFullYear(),month:viewMonth.getMonth()}}
function allStores(){try{return state.stores||[]}catch(e){return[]}}
function resolveStore(x){try{return allStores().find(s=>String(s.id)===String(x.id))||x}catch(e){return x}}
function routeKm(route){if(!route||!route.length)return 0;try{let km=typeof havBase==='function'?havBase(route[0]):0;for(let i=1;i<route.length;i++)km+=(typeof hav==='function'?hav(route[i-1],route[i]):0);if(typeof hav==='function'&&typeof baseObj==='function')km+=hav(route[route.length-1],baseObj());return km}catch(e){return 0}}
function routeMinutes(route){return routeKm(route)*1.22/55*60+(route?route.length:0)*Number((state.settings&&state.settings.visitMinutes)||60)}
function routeFinish(route,day){return mins(day==='Samedi'?((state.settings&&state.settings.saturdayStart)||'08:00'):((state.settings&&state.settings.startTime)||'08:30'))+routeMinutes(route)}
function limitForDay(day){return mins(day==='Samedi'?((state.settings&&state.settings.saturdayEnd)||'12:00'):((state.settings&&state.settings.endTime)||'18:00'))}
function intervalDays(s){return Number(s.intervalDays||(/hebdo/i.test(String(s.freq||''))?7:/bi-mens|quinz/i.test(String(s.freq||''))?14:30))}
function daysSince(v){if(!v)return 9999;const d=parseDate(v);return d?Math.floor((new Date()-d)/86400000):9999}
function selectedDay(){const a=document.querySelector('#dayTabs .dayTab.active');if(a){for(const d of DAYS)if(norm(a.textContent).includes(norm(d)))return d}try{return window.selectedPlanningDay||((state.settings&&state.settings.days)||DAYS)[0]}catch(e){return'Lundi'}}
function brandClass(v){const n=norm(v);for(const b of ['boulanger','darty','fnac','carrefour','conforama','cuisinella'])if(n.includes(b))return b;return'other'}
function monthEntries(){const target=currentMonth(),a=archive(),out=[];for(const key of Object.keys(a)){const w=a[key];if(!w||!w.plan)continue;const mon=parseDate(w.weekMonday||key);if(!mon)continue;for(let i=0;i<DAYS.length;i++){const d=addDays(mon,i);if(d.getFullYear()===target.year&&d.getMonth()===target.month)out.push({date:d,day:DAYS[i],route:w.plan[DAYS[i]]||[]})}}return out.sort((a,b)=>a.date-b.date)}
function monthStats(){const entries=monthEntries();let visits=0,km=0,minutes=0;const unique=new Set();for(const e of entries){visits+=e.route.length;km+=routeKm(e.route);minutes+=routeMinutes(e.route);e.route.forEach(s=>unique.add(String(s.id||s.enseigne+'|'+s.ville)))}let hotels=0;try{for(const r of (state.awayRanges||[])){const d=parseDate(r.start||r.date),m=currentMonth();if(d&&d.getFullYear()===m.year&&d.getMonth()===m.month)hotels++}}catch(e){}return{entries,visits,km,minutes,unique:unique.size,hotels}}
function alerts(){const out=[],days=(state.settings&&state.settings.days)||DAYS.slice(0,5);for(const day of days){const r=(state.plan&&state.plan[day])||[];if(!r.length)continue;const f=routeFinish(r,day),l=limitForDay(day);if(f>l)out.push({type:'bad',text:day+' finit vers '+clock(f)+' alors que ta limite est '+clock(l)+'.'});else if(l-f<30)out.push({type:'warn',text:day+' est presque plein : fin estimée '+clock(f)+'.'})}const planned=new Set();for(const d of DAYS)((state.plan&&state.plan[d])||[]).forEach(s=>planned.add(String(s.id)));const late=[];for(const s of allStores()){const n=daysSince(s.lastVisit)-intervalDays(s);if(n>14&&!planned.has(String(s.id)))late.push({s,n})}late.sort((a,b)=>b.n-a.n).slice(0,4).forEach(x=>out.push({type:'warn',text:(x.s.enseigne||'Magasin')+' '+(x.s.ville||'')+' est en retard d’environ '+x.n+' jours et n’est pas dans la semaine.'}));return out}
function quality(){let score=100;for(const x of alerts())score-=x.type==='bad'?12:5;score=Math.max(0,score);return{score,label:score>=85?'Très bon':score>=70?'Correct':score>=50?'À optimiser':'Trop chargé'}}
function eventMap(){const map={};try{for(const e of (state.calendarEvents||[])){const raw=e.date||e.startDate||(e.start&&e.start.dateTime)||(e.start&&e.start.date),k=raw?String(raw).slice(0,10):'';if(k)(map[k]||(map[k]=[])).push(e)}}catch(e){}return map}
function loadMonthDay(raw){const date=parseDate(raw);if(!date||date.getDay()===0)return;const a=archive(),mon=monday(date),key=iso(mon),snap=a[key],day=DAYS[date.getDay()-1];if(snap&&snap.plan){state.plan={};for(const d of DAYS)state.plan[d]=(snap.plan[d]||[]).map(resolveStore)}try{if(!state.settings)state.settings={};state.settings.weekDate=key;const w=document.getElementById('weekDate');if(w)w.value=key;window.selectedPlanningDay=day;if(typeof save==='function')save();if(typeof renderAll==='function')renderAll()}catch(e){}setTimeout(function(){const tab=document.querySelector('#dayTabs [data-date="'+raw+'"]');if(tab&&typeof tab.click==='function')tab.click();const target=document.querySelector('.timelineShell')||document.getElementById('dayTabs');if(target)target.scrollIntoView({behavior:'smooth',block:'start'})},120)}
function shiftMonth(delta){const m=currentMonth();viewMonth=new Date(m.year,m.month+delta,1,12);renderMonth()}
function renderMonth(){const box=document.getElementById('proMonthBody');if(!box)return;const m=currentMonth(),stats=monthStats(),emap=eventMap(),first=new Date(m.year,m.month,1,12),last=new Date(m.year,m.month+1,0,12);let start=new Date(first),dow=(start.getDay()+6)%7;start.setDate(start.getDate()-dow);const cells=Math.ceil((dow+last.getDate())/7)*7,by={};stats.entries.forEach(e=>by[iso(e.date)]=e.route);let h='<div class="proMonthNav"><button class="secondary proMonthPrev" type="button" aria-label="Mois précédent">‹</button><div class="proMonthHead"><b>'+first.toLocaleDateString('fr-FR',{month:'long',year:'numeric'})+'</b><span>'+stats.visits+' visites · '+stats.unique+' magasins distincts</span></div><button class="secondary proMonthNext" type="button" aria-label="Mois suivant">›</button></div><div class="proSwipeHint">Glisse à gauche ou à droite pour changer de mois · touche un jour pour l’ouvrir</div><div class="proWeekdays">'+['Lun','Mar','Mer','Jeu','Ven','Sam','Dim'].map(x=>'<span>'+x+'</span>').join('')+'</div><div class="proMonthGrid">';for(let i=0;i<cells;i++){const d=addDays(start,i),key=iso(d),inside=d.getMonth()===m.month,route=by[key]||[],evs=emap[key]||[],clickable=inside&&d.getDay()!==0;h+='<button type="button" class="proDay '+(inside?'':'muted')+(clickable?' clickable':'')+'" '+(clickable?'data-pro-date="'+key+'"':'disabled')+'><div class="proDate">'+d.getDate()+'</div>';for(const s of route.slice(0,4))h+='<div class="proStore '+brandClass(s.enseigne)+'">'+esc((s.enseigne||'')+' '+(s.ville||''))+'</div>';if(route.length>4)h+='<div class="proMore">+'+(route.length-4)+' visites</div>';for(const e of evs.slice(0,1))h+='<div class="proEvent">▣ '+esc(e.title||e.summary||'Agenda')+'</div>';h+='</button>'}box.innerHTML=h+'</div>';const prev=box.querySelector('.proMonthPrev'),next=box.querySelector('.proMonthNext');if(prev)prev.onclick=()=>shiftMonth(-1);if(next)next.onclick=()=>shiftMonth(1);box.querySelectorAll('[data-pro-date]').forEach(b=>b.onclick=()=>loadMonthDay(b.dataset.proDate));box.ontouchstart=function(e){touchStartX=e.touches&&e.touches[0]?e.touches[0].clientX:null};box.ontouchend=function(e){if(touchStartX==null)return;const x=e.changedTouches&&e.changedTouches[0]?e.changedTouches[0].clientX:touchStartX,dx=x-touchStartX;touchStartX=null;if(Math.abs(dx)>55)shiftMonth(dx<0?1:-1)}}
function render(){const q=quality(),st=monthStats(),a=alerts(),qe=document.getElementById('proQuality'),me=document.getElementById('proMonthMetrics'),ae=document.getElementById('proAlerts');if(qe)qe.innerHTML='<div class="proScore">'+q.score+'<small>/100</small></div><div><b>'+q.label+'</b><span>charge, horaires et couverture de la semaine</span></div>';if(me)me.innerHTML='<div><b>'+st.visits+'</b><span>visites mois</span></div><div><b>'+Math.round(st.km)+'</b><span>km estimés</span></div><div><b>'+Math.round(st.minutes/60)+'</b><span>h terrain + route</span></div><div><b>'+st.hotels+'</b><span>découchés</span></div>';if(ae)ae.innerHTML=a.length?a.map(x=>'<div class="proAlert '+x.type+'">'+esc(x.text)+'</div>').join(''):'<div class="proAlert ok">Aucune alerte importante sur la semaine affichée.</div>';renderMonth()}
function optimizeDay(){const day=selectedDay();try{if(typeof window.regenerateDay==='function')window.regenerateDay(day);else if(typeof regenerateDay==='function')regenerateDay(day);else if(typeof generateWeek==='function')generateWeek();setTimeout(render,150)}catch(e){console.warn(e)}}
function printMonth(){renderMonth();document.body.classList.add('printProMonth');setTimeout(()=>{window.print();setTimeout(()=>document.body.classList.remove('printProMonth'),400)},50)}
function cleanupStray(){try{const w=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT),kill=[];let n;while(n=w.nextNode()){const t=String(n.nodeValue||'').trim();if(/^(\\n)+$/.test(t)||t==='\\n\\n')kill.push(n)}kill.forEach(n=>n.remove())}catch(e){}}
function css(){if(document.getElementById('planningProPlusCss'))return;const s=document.createElement('style');s.id='planningProPlusCss';s.textContent='.proCard{background:#fff;border:1px solid #e1e5ed;border-radius:18px;padding:14px;margin:0 0 12px;box-shadow:0 4px 14px rgba(25,42,80,.045)}#planningProTop{margin:0 0 14px}.proTop{display:grid;grid-template-columns:1fr 1fr;gap:12px}.proActions,.proTitle,.proMonthTools,.proMonthHead,.proMonthNav{display:flex;justify-content:space-between;gap:10px;align-items:center}.proActions span,.proTitle span,.proMonthTools span,.proMonthHead span{display:block;font-size:10px;color:#667085}.proMonthNav>.secondary{flex:0 0 38px;width:38px;height:38px;padding:0;border-radius:12px;font-size:25px;line-height:1}.proMonthHead{flex:1;min-width:0}.proSwipeHint{text-align:center;font-size:9px;color:#98a2b3;margin:5px 0 9px}.proQuality{display:flex;align-items:center;gap:12px;margin-top:8px}.proScore{font-size:30px;font-weight:850}.proScore small{font-size:11px;color:#98a2b3}.proMetrics{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:8px}.proMetrics div{background:#f7f9fc;border-radius:11px;padding:8px}.proMetrics b{display:block;font-size:17px}.proMetrics span{font-size:9px;color:#667085}.proAlert{padding:9px 10px;border-radius:11px;margin-top:7px;font-size:11px;line-height:1.35}.proAlert.warn{background:#fff8e8;color:#7a4b00}.proAlert.bad{background:#fff2f0;color:#9e2d23}.proAlert.ok{background:#effbf6;color:#177456}.proMonthCard{margin-top:14px}.proMonthCard summary{font-weight:800;cursor:pointer}.proMonthTools{margin:12px 0}.proWeekdays,.proMonthGrid{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:5px}.proWeekdays span{text-align:center;font-size:9px;color:#667085;padding:3px}.proDay{appearance:none;text-align:left;color:inherit;font:inherit;min-width:0;min-height:78px;border:1px solid #e7eaf0;border-radius:10px;padding:5px;background:#fff;overflow:hidden}.proDay.clickable{cursor:pointer;touch-action:manipulation}.proDay.clickable:active{transform:scale(.97);background:#f5f8ff}.proDay.muted{opacity:.28;background:#f8f9fb}.proDay:disabled{color:inherit}.proDate{font-size:10px;font-weight:800;margin-bottom:4px}.proStore,.proEvent,.proMore{font-size:8.5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;border-radius:7px;padding:3px 4px;margin:2px 0}.proStore.boulanger{background:#fff3cc;color:#8a6200}.proStore.darty{background:#ffeaea;color:#a92a2a}.proStore.fnac{background:#eaf3ff;color:#1769d2}.proStore.carrefour,.proStore.conforama{background:#fff0e8;color:#a54a18}.proStore.cuisinella{background:#f1ebff;color:#6941c6}.proEvent{background:#eaf3ff;color:#1769d2}.proMore{background:#f2f4f7;color:#667085}@media(max-width:700px){.proTop{grid-template-columns:1fr}.proActions{align-items:flex-start}.proMetrics{grid-template-columns:repeat(2,1fr)}.proDay{min-height:70px;padding:4px}.proStore,.proEvent,.proMore{font-size:8px;padding:2px 3px}.proMonthHead{align-items:center;flex-direction:column;gap:2px;text-align:center}.proMonthNav{gap:6px}}@media print{body.printProMonth *{visibility:hidden!important}body.printProMonth #planningProMonth,body.printProMonth #planningProMonth *{visibility:visible!important}body.printProMonth #planningProMonth{position:absolute;inset:0;margin:0;background:#fff}.bottomAppNav{display:none!important}}';document.head.appendChild(s)}
function install(){const plan=document.getElementById('planPanel'),apple=plan&&plan.querySelector('.applePlan');if(!apple)return false;css();const old=document.getElementById('planningProPlus');if(old)old.remove();if(!document.getElementById('planningProTop')){const top=document.createElement('div');top.id='planningProTop';top.innerHTML='<div class="proCard"><div class="proActions"><div><b>Alertes & recommandations</b><span>ce qui mérite ton attention avant de partir</span></div><button id="proOptimizeDay" class="secondary" type="button">Optimiser cette journée</button></div><div id="proAlerts"></div></div><div class="proTop"><div class="proCard"><div class="proTitle"><b>Qualité du planning</b><span>semaine affichée</span></div><div id="proQuality" class="proQuality"></div></div><div class="proCard"><div class="proTitle"><b>Résumé du mois</b><span>activité estimée</span></div><div id="proMonthMetrics" class="proMetrics"></div></div></div>';const metrics=document.getElementById('planMetrics');apple.insertBefore(top,metrics||apple.firstChild);document.getElementById('proOptimizeDay').addEventListener('click',optimizeDay)}if(!document.getElementById('planningProMonth')){const month=document.createElement('details');month.id='planningProMonth';month.className='proCard proMonthCard';month.open=true;month.innerHTML='<summary>Vue mensuelle du planning</summary><div class="proMonthTools"><span>Planning + événements Google Agenda</span><button id="proPrintMonth" class="secondary" type="button">Exporter / PDF</button></div><div id="proMonthBody"></div>';const timeline=apple.querySelector('.timelineShell');if(timeline)timeline.insertAdjacentElement('afterend',month);else apple.appendChild(month);document.getElementById('proPrintMonth').addEventListener('click',printMonth)}cleanupStray();render();return true}
function scheduleInstall(delay){clearTimeout(refreshTimer);refreshTimer=setTimeout(function(){install();observeRefreshTargets()},delay==null?45:delay)}
function observeRefreshTargets(){
  if(!refreshObserver)refreshObserver=new MutationObserver(function(records){for(const r of records){if((r.addedNodes&&r.addedNodes.length)||(r.removedNodes&&r.removedNodes.length)||r.type==='characterData'){scheduleInstall(45);return}}});
  const targets=[document.getElementById('dayTabs'),document.querySelector('#planPanel .timelineShell')].filter(Boolean);
  targets.forEach(function(target){if(observedRefreshTargets.has(target))return;refreshObserver.observe(target,{childList:true,subtree:true,characterData:true});observedRefreshTargets.add(target)});
  return targets.length>0;
}
function boot(){if(install()){observeRefreshTargets();return}[100,250,600,1200,2400].forEach(function(delay){setTimeout(function(){install();observeRefreshTargets()},delay)})}
window.addEventListener('chef-range-generated',function(e){const s=e&&e.detail&&e.detail.start;if(s){const d=parseDate(s);if(d)viewMonth=new Date(d.getFullYear(),d.getMonth(),1,12)}setTimeout(function(){install();observeRefreshTargets()},80)});
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
window.addEventListener('load',function(){install();observeRefreshTargets()},{once:true});
window.addEventListener('focus',function(){install();observeRefreshTargets()});
document.addEventListener('visibilitychange',function(){if(!document.hidden){install();observeRefreshTargets()}});
document.addEventListener('click',function(e){if(e.target&&e.target.closest&&e.target.closest('#dayTabs .dayTab'))setTimeout(function(){install();observeRefreshTargets()},80)},true);
})();