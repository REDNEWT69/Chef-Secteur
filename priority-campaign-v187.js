/* V187 · Campagne terrain prioritaire : faible / anomalie de part de marché services. */
(function(root){
'use strict';
const DAYS=['Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi'];
const ARCHIVE_KEY='chef_sector_plan_archive_v1';
const CAMPAIGN={
  id:'services-p1-2026-09',
  label:'Priorités services',
  reason:'Anomalie / faible part de marché services',
  startDate:'2026-09-14',
  dueDate:'2026-09-22',
  targets:[
    {key:'boulanger-aubiere',brand:'Boulanger',label:'Boulanger Aubière / Clermont',aliases:['aubiere','clermont'],order:1},
    {key:'boulanger-villars',brand:'Boulanger',label:'Boulanger Villars / Saint-Étienne',aliases:['villars','saint etienne','st etienne'],order:2},
    {key:'boulanger-valence',brand:'Boulanger',label:'Boulanger Valence',aliases:['valence'],order:3},
    {key:'boulanger-chambery',brand:'Boulanger',label:'Boulanger Chambéry',aliases:['chambery'],order:4},
    {key:'boulanger-annemasse',brand:'Boulanger',label:'Boulanger Annemasse',aliases:['annemasse'],order:5},
    {key:'auchan-saint-priest',brand:'Auchan',label:'Auchan Saint-Priest',aliases:['saint priest'],order:6,creditedDate:'2026-09-14'},
    {key:'but-saint-priest',brand:'BUT',label:'BUT Lyon Saint-Priest',aliases:['saint priest'],order:7,creditedDate:'2026-09-15'}
  ]
};
let applying=false,lastReport=null,bootAttempts=0;
function clone(v){return JSON.parse(JSON.stringify(v))}
function norm(v){try{return String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim()}catch(e){return String(v||'').toLowerCase().trim()}}
function parse(v){const d=new Date(String(v||'')+'T12:00:00');return isNaN(d)?null:d}
function iso(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}
function monday(d){const x=new Date(d),w=x.getDay()||7;x.setDate(x.getDate()-w+1);return x}
function dateLabel(value){const d=parse(value);if(!d)return value||'';return String(d.getDate()).padStart(2,'0')+'/'+String(d.getMonth()+1).padStart(2,'0')}
function storage(){try{return root.__chefStorage||root.localStorage||null}catch(e){return null}}
function loadArchive(){try{const s=storage();return s?JSON.parse(s.getItem(ARCHIVE_KEY)||'{}')||{}:{}}catch(e){return{}}}
function saveArchive(v){try{const s=storage();if(s)s.setItem(ARCHIVE_KEY,JSON.stringify(v||{}))}catch(e){console.warn('Archive P1 non enregistrée',e)}}
function storeHay(store){return norm([store&&store.enseigne,store&&store.sourceName,store&&store.ville,store&&store.adresse].filter(Boolean).join(' '))}
function targetMatches(target,store){if(!target||!store)return false;const brand=norm(store.enseigne),wanted=norm(target.brand);if(brand!==wanted&&!brand.includes(wanted))return false;const hay=storeHay(store);return (target.aliases||[]).some(a=>hay.includes(norm(a)))}
function currentStores(){try{return Array.isArray(root.state&&root.state.stores)?root.state.stores:[]}catch(e){return[]}}
function snapshotStores(){try{return root.state&&root.state.businessV2&&root.state.businessV2.storeSnapshots?Object.values(root.state.businessV2.storeSnapshots):[]}catch(e){return[]}}
function resolveTarget(target){return currentStores().find(s=>targetMatches(target,s))||null}
function visitDatesForId(id){const out=[];try{const legacy=root.state&&root.state.visits&&root.state.visits[String(id)];if(legacy){if(legacy.lastVisit)out.push(String(legacy.lastVisit).slice(0,10));if(Array.isArray(legacy.history))legacy.history.forEach(d=>out.push(String(d||'').slice(0,10)))}}catch(e){}
 try{const rows=root.state&&root.state.businessV2&&Array.isArray(root.state.businessV2.visits)?root.state.businessV2.visits:[];rows.forEach(v=>{if(String(v&&v.storeId)===String(id)&&String(v&&v.status)==='completed')out.push(String((v&&v.completedDate)||(v&&v.completedAt)||'').slice(0,10))})}catch(e){}
 return [...new Set(out.filter(d=>/^\d{4}-\d{2}-\d{2}$/.test(d)))].sort()
}
function actualCompletedDate(target){const candidates=currentStores().concat(snapshotStores()).filter(s=>targetMatches(target,s));let dates=[];candidates.forEach(s=>{if(s&&s.id!==undefined)dates=dates.concat(visitDatesForId(s.id))});dates=[...new Set(dates)].filter(d=>d>=CAMPAIGN.startDate&&d<=CAMPAIGN.dueDate).sort();return dates[0]||''}
function completionDate(target){return actualCompletedDate(target)||target.creditedDate||''}
function workDays(){try{const days=root.state&&root.state.settings&&Array.isArray(root.state.settings.days)?root.state.settings.days.filter(d=>DAYS.includes(d)):[];return days.length?days:DAYS.slice(0,5)}catch(e){return DAYS.slice(0,5)}}
function weekKeyFromState(){try{return iso(monday(parse(root.state&&root.state.settings&&root.state.settings.weekDate)||new Date()))}catch(e){return iso(monday(new Date()))}}
function emptyPlan(){return Object.fromEntries(DAYS.map(d=>[d,[]]))}
function planForWeek(key,archive){if(key===weekKeyFromState()&&root.state&&root.state.plan)return clone(root.state.plan);const snap=archive&&archive[key];return snap&&snap.plan?clone(snap.plan):emptyPlan()}
function targetForStore(store){return CAMPAIGN.targets.find(t=>targetMatches(t,store))||null}
function isPriorityStore(store){return !!targetForStore(store)}
function planningCredit(store){try{if(typeof root.storeVisitCredit==='function')return Math.max(1,Number(root.storeVisitCredit(store))||1)}catch(e){}return 1}
function routeCredits(route){return (route||[]).reduce((n,s)=>n+planningCredit(s),0)}
function maxCredits(){try{return Math.max(1,Math.min(8,Number(root.state&&root.state.settings&&root.state.settings.maxVisitsPerDay)||4))}catch(e){return 4}}
function visitedOn(id,date){return visitDatesForId(id).includes(date)}
function lockDay(id,weekKey){try{if(typeof root.storeRunnerLockDayForWeek==='function'){const d=root.storeRunnerLockDayForWeek(id,weekKey);if(DAYS.includes(d))return d}}catch(e){}try{const raw=root.state&&root.state.locks&&root.state.locks[String(id)];if(typeof raw==='string')return DAYS.includes(raw)?raw:'';if(raw&&typeof raw==='object'&&DAYS.includes(raw.day)&&(!raw.week||String(raw.week)===String(weekKey)))return raw.day}catch(e){}return''}
function appointmentDay(id,weekKey){try{for(const a of (root.state&&root.state.appointments||[])){if(String(a&&a.storeId)!==String(id))continue;const d=parse(String(a&&a.date||'').slice(0,10));if(!d||iso(monday(d))!==weekKey)continue;return DAYS[(d.getDay()||7)-1]||''}}catch(e){}return''}
function fixedOnDay(store,date,day,weekKey){const id=String(store&&store.id||'');return !!(id&&(visitedOn(id,date)||appointmentDay(id,weekKey)===day||lockDay(id,weekKey)===day))}
function eventBlocks(e){if(!e)return false;if(e.inferredAway)return true;const text=norm((e.title||'')+' '+(e.location||'')+' '+(e.calendar||''));return ['formation','deplacement','seminaire','conge','vacances','salon professionnel','indisponible','indisponibilite','absence','absent','journee bloquee','jour bloque','repos','hors secteur'].some(x=>text.includes(x))||/\bparis\b/.test(text)||!!(e.planningBlock&&!e.allDay)}
function dateBlocked(date){try{const rows=typeof root.calendarEventsForDate==='function'?root.calendarEventsForDate(date):[];return (rows||[]).some(eventBlocks)}catch(e){return false}}
function routeFits(route,day,weekKey){try{const api=root.StoreOpeningHoursV1;if(api&&typeof api.routeFits==='function')return !!api.routeFits(route,day,root.state,{weekMonday:parse(weekKey)})}catch(e){}
 try{if(typeof root.routeWorkMinutes!=='function')return true;const settings=root.state&&root.state.settings||{},clock=v=>{const p=String(v||'').split(':');return (+p[0]||0)*60+(+p[1]||0)},start=clock(day==='Samedi'?(settings.saturdayStart||'08:00'):(settings.startTime||'08:30')),end=clock(day==='Samedi'?(settings.saturdayEnd||'12:00'):(settings.endTime||'18:00'));return start+Number(root.routeWorkMinutes(route)||0)<=end+0.001}catch(e){return true}}
function routeKm(route){try{const api=root.StoreRunnerGeographyV185;if(api&&typeof api.routeKm==='function'){const n=Number(api.routeKm(route));if(Number.isFinite(n))return n}}catch(e){}return (route||[]).length*100}
function optimizeRoute(route){try{if(typeof root.nearestRoute==='function'&&typeof root.twoOpt==='function')return root.twoOpt(root.nearestRoute(route));if(typeof root.nearestRoute==='function')return root.nearestRoute(route)}catch(e){}return (route||[]).slice()}
function weekPlansBetween(today){const archive=loadArchive(),start=monday(today),end=monday(parse(CAMPAIGN.dueDate)),plans={};for(let d=new Date(start);d<=end;d=addDays(d,7)){const key=iso(d);plans[key]=planForWeek(key,archive)}return{archive,plans}}
function routeContext(plans,date){const d=parse(date),key=iso(monday(d)),day=DAYS[(d.getDay()||7)-1];return{date,key,day,plan:plans[key],route:plans[key]&&plans[key][day]||[]}}
function allDates(from,to,allowedDays){const out=[];for(let d=new Date(from);d<=to;d=addDays(d,1)){const day=DAYS[(d.getDay()||7)-1];if(day&&allowedDays.includes(day))out.push(iso(d))}return out}
function scheduledDateForTarget(target,plans,limitDue=true){for(const [key,plan] of Object.entries(plans)){const mon=parse(key);for(const day of DAYS){const date=iso(addDays(mon,DAYS.indexOf(day)));if(limitDue&&date>CAMPAIGN.dueDate)continue;if((plan[day]||[]).some(s=>targetMatches(target,s)))return date}}return''}
function removeFutureCompleted(plans,today){let removed=0;for(const target of CAMPAIGN.targets){const done=completionDate(target);if(!done)continue;for(const [key,plan] of Object.entries(plans)){const mon=parse(key);for(const day of DAYS){const date=iso(addDays(mon,DAYS.indexOf(day)));if(date<today||date<=done)continue;const before=(plan[day]||[]).length;plan[day]=(plan[day]||[]).filter(s=>!targetMatches(target,s));removed+=before-plan[day].length}}}return removed}
function bestInsertion(target,store,plans,dates,preferredIndex){const max=maxCredits();let best=null;for(let i=0;i<dates.length;i++){const ctx=routeContext(plans,dates[i]);if(!ctx.plan||dateBlocked(ctx.date))continue;const base=(ctx.route||[]).slice(),removable=[];for(let j=0;j<base.length;j++){const row=base[j];if(isPriorityStore(row)||fixedOnDay(row,ctx.date,ctx.day,ctx.key))continue;removable.push(j)}
 const totalMasks=1<<Math.min(removable.length,12);for(let mask=0;mask<totalMasks;mask++){const removedIdx=new Set(),removed=[];for(let b=0;b<removable.length;b++)if(mask&(1<<b)){removedIdx.add(removable[b]);removed.push(base[removable[b]])}const kept=base.filter((_,idx)=>!removedIdx.has(idx));const trial=optimizeRoute(kept.concat([store]));if(routeCredits(trial)>max||!routeFits(trial,ctx.day,ctx.key))continue;const incremental=Math.max(0,routeKm(trial)-routeKm(kept)),score=removed.length*100000+Math.abs(i-preferredIndex)*250+incremental;const row={score,date:ctx.date,key:ctx.key,day:ctx.day,trial,removed};if(!best||row.score<best.score)best=row;if(!removed.length&&Math.abs(i-preferredIndex)===0)break}
 }
 return best}
function placeDeferred(stores,plans,today){const due=parse(CAMPAIGN.dueDate),end=addDays(monday(due),5),all=allDates(today,end,workDays()),dates=all.sort((a,b)=>((a>CAMPAIGN.dueDate?0:1)-(b>CAMPAIGN.dueDate?0:1))||a.localeCompare(b)),unplaced=[];for(const store of stores){let best=null;for(const date of dates){const ctx=routeContext(plans,date);if(!ctx.plan||dateBlocked(date))continue;const trial=optimizeRoute((ctx.route||[]).concat([store]));if(routeCredits(trial)>maxCredits()||!routeFits(trial,ctx.day,ctx.key))continue;const score=Math.max(0,routeKm(trial)-routeKm(ctx.route||[]));if(!best||score<best.score)best={score,ctx,trial}}if(best){best.ctx.plan[best.ctx.day]=best.trial}else unplaced.push(store)}return unplaced}
function persist(plans,archive,changedKeys){const currentKey=weekKeyFromState(),at=new Date().toISOString();if(changedKeys.has(currentKey)&&root.state)root.state.plan=Object.fromEntries(DAYS.map(day=>[day,(plans[currentKey]&&plans[currentKey][day]||[]).map(row=>currentStores().find(s=>String(s.id)===String(row&&row.id))||row)]));for(const key of changedKeys){const previous=archive[key]||{weekMonday:key};archive[key]=Object.assign({},previous,{weekMonday:key,plan:clone(plans[key]||emptyPlan()),updatedAt:at,priorityCampaign:CAMPAIGN.id})}saveArchive(archive);try{if(root.ChefReliability&&typeof root.ChefReliability.checkpoint==='function')root.ChefReliability.checkpoint('Avant application des priorités services V187',storage())}catch(e){}try{if(typeof root.save==='function')root.save()}catch(e){}try{if(typeof root.renderAll==='function')root.renderAll()}catch(e){}try{const s=storage();if(s&&typeof s.flush==='function'){const p=s.flush();if(p&&typeof p.catch==='function')p.catch(()=>{})}}catch(e){}}
function statusFromPlans(plans){return CAMPAIGN.targets.map(target=>{const completed=completionDate(target),store=resolveTarget(target),planned=completed?'':scheduledDateForTarget(target,plans,true);return{key:target.key,label:target.label,completedDate:completed,plannedDate:planned,resolved:!!store,storeId:store&&String(store.id)}})}
function renderSummary(){if(!root.document||!root.state)return false;const data=weekPlansBetween(new Date()),rows=statusFromPlans(data.plans),done=rows.filter(r=>r.completedDate).length,planned=rows.filter(r=>!r.completedDate&&r.plannedDate).length,pending=rows.length-done-planned;let box=root.document.getElementById('priorityCampaignV187');if(!box){const anchor=root.document.getElementById('planningUnifiedHint')||root.document.querySelector&&root.document.querySelector('#planPanel button.primary.full[onclick="generateWeek()"]');if(!anchor||!anchor.parentNode)return false;box=root.document.createElement('details');box.id='priorityCampaignV187';box.open=true;box.style.cssText='margin:10px 0;padding:10px 12px;border:1px solid #fecaca;border-radius:14px;background:#fff7f7;color:#7f1d1d;font-size:12px;line-height:1.45';anchor.insertAdjacentElement('afterend',box)}const summary=done+'/'+rows.length+' réalisées · '+planned+' planifiée'+(planned>1?'s':'')+(pending?' · '+pending+' à placer':'')+' · échéance 22/09';box.innerHTML='<summary style="cursor:pointer;font-weight:850">🔴 Priorités services · '+summary+'</summary><div style="margin-top:8px;color:#475467">'+rows.map(r=>r.completedDate?'✅ '+r.label+' · visitée '+dateLabel(r.completedDate):r.plannedDate?'📍 '+r.label+' · '+dateLabel(r.plannedDate):'🔴 '+r.label+' · à visiter avant le 22/09').join('<br>')+'</div>';return true}
function apply(options){options=options||{};if(applying||!root.state||!Array.isArray(root.state.stores))return{ok:false,reason:'not-ready'};applying=true;try{const today=parse(options.today)||new Date();const todayIso=iso(today);if(todayIso>CAMPAIGN.dueDate){const data=weekPlansBetween(today);lastReport={ok:true,expired:true,status:statusFromPlans(data.plans)};if(options.render!==false)renderSummary();return lastReport}
 const data=weekPlansBetween(today),archive=data.archive,plans=data.plans,beforePlans=clone(data.plans),changedKeys=new Set();removeFutureCompleted(plans,todayIso);const rows=statusFromPlans(plans),pending=rows.filter(r=>!r.completedDate&&!r.plannedDate),dates=allDates(today,parse(CAMPAIGN.dueDate),workDays()).filter(d=>!dateBlocked(d)),deferred=[],unresolved=[];pending.sort((a,b)=>{const ta=CAMPAIGN.targets.find(t=>t.key===a.key),tb=CAMPAIGN.targets.find(t=>t.key===b.key);return ta.order-tb.order});for(let p=0;p<pending.length;p++){const row=pending[p],target=CAMPAIGN.targets.find(t=>t.key===row.key),store=resolveTarget(target);if(!store){unresolved.push(target.label);continue}const preferred=Math.min(dates.length-1,p),choice=bestInsertion(target,store,plans,dates,preferred);if(!choice){unresolved.push(target.label+' (aucun créneau compatible)');continue}choice.removed.forEach(s=>deferred.push(s));plans[choice.key][choice.day]=choice.trial}
 const dedupDeferred=[],seenDeferred=new Set();deferred.forEach(s=>{const id=String(s&&s.id||'');if(id&&!seenDeferred.has(id)){seenDeferred.add(id);dedupDeferred.push(s)}});const deferredUnplaced=placeDeferred(dedupDeferred,plans,today);for(const key of Object.keys(plans)){if(JSON.stringify(plans[key]||{})!==JSON.stringify(beforePlans[key]||{}))changedKeys.add(key)}if(changedKeys.size)persist(plans,archive,changedKeys);const finalRows=statusFromPlans(plans),done=finalRows.filter(r=>r.completedDate).length,planned=finalRows.filter(r=>!r.completedDate&&r.plannedDate).length;lastReport={ok:true,campaign:CAMPAIGN.id,done,total:CAMPAIGN.targets.length,planned,pending:CAMPAIGN.targets.length-done-planned,unresolved,deferred:dedupDeferred.length,deferredUnplaced:deferredUnplaced.map(s=>String((s&&s.enseigne)||'Magasin')+' '+String((s&&s.ville)||'')),status:finalRows};try{if(changedKeys.size&&typeof root.storeRunnerRefreshOvernightDecision==='function')root.storeRunnerRefreshOvernightDecision(root.state.plan)}catch(e){}if(options.render!==false)renderSummary();if(options.emit!==false&&changedKeys.size&&root.document&&typeof root.CustomEvent==='function')try{root.document.dispatchEvent(new root.CustomEvent('store-runner:planning-updated',{detail:{source:'priority-v187',campaign:CAMPAIGN.id}}))}catch(e){}return lastReport
 }finally{applying=false}}
function scheduleApply(source){if(applying)return;root.setTimeout?root.setTimeout(()=>{const result=apply({});if(result&&result.ok&&result.pending&&source==='generateWeek'&&typeof root.storeRunnerToast==='function')try{root.storeRunnerToast('Priorités services : '+result.pending+' magasin(s) restent à placer avant le 22/09.')}catch(e){}},0):apply({})}
function install(){if(!root.state||!Array.isArray(root.state.stores)){if(root.setTimeout&&bootAttempts++<40)root.setTimeout(install,100);return false}renderSummary();scheduleApply('boot');return true}
if(root.document){if(root.document.readyState==='loading')root.document.addEventListener('DOMContentLoaded',install,{once:true});else install();root.document.addEventListener('store-runner:planning-updated',function(e){const source=e&&e.detail&&e.detail.source;if(source==='priority-v187'){renderSummary();return}if(['generateWeek','recalculateRemainingWeek','single-week-geo-v185'].includes(source))scheduleApply(source);else renderSummary()});root.document.addEventListener('store-runner:data-restored',()=>scheduleApply('restore'));root.document.addEventListener('store-runner:home-rendered',renderSummary)}
const api={campaign:CAMPAIGN,apply,status(){const data=weekPlansBetween(new Date());return statusFromPlans(data.plans)},render:renderSummary,targetMatches,completionDate,getLastReport(){return lastReport}};
root.StoreRunnerPriorityCampaignV187=api;if(typeof module!=='undefined'&&module.exports)module.exports=api;
})(typeof window!=='undefined'?window:globalThis);